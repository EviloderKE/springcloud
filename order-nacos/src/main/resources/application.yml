server:
  port: 8003
spring:
  application:
    name: nacos-order
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    com.example.order.service.FeignNacosService: debug