server:
  port: 9000

spring:
  application:
    name: payment
  cloud:
    config:
      label: master
      name: order
      profile: dev
      uri: http://localhost:7100

management:
  endpoints:
    web:
      exposure:
        include: "*"